---
title : "[OZ 게임개발캠프] 6일차 (03/26)"
date : 2026-03-26 14:31:00 +0900
categories : [OZ]
tags : [ozcodingschool]
comments : true
pin : true
---

## [OZ 게임개발캠프] 6일차 (03 / 26)

### **1. 객체**
- **클래스** : **데이터와 기능**으로 구성이 되어있다.
	- 보통 데이터 → 함수 순으로 구성
- **객체** : 현실 세계의 모든 개체를 클래스의 형태로 표현한 것
	- 클래스는 설계도, 객체는 그 설계도로 제작된 실제 개체
- **인스턴스** : 객체를 프로그램에서 직접 사용할 수 있도록 실체화하는 것
	- **new** 키워드로 인스턴스화
	- **delete** : 소멸자를 자동으로 불러와 객체 제거

```
함수 : main() 함수 밖에 선언된 함수
메서드 : 클래스 안에서 선언된 함수
```

#### **예시**
```cpp
class Example
{
public:
	string name;
	int age;
	double height, weight;

	void ShowAge()
	{
		cout << age;
	}
};
```

<br/>

-----------------

<br/>

### **2. 접근 제한자 & Getter/Setter**
- **접근 제한자** : 외부의 접근 권한을 분류하여 객체를 보호하기 위한 키워드
	- 클래스, 구조체, 언어에 따라 기본으로 부여되는 접근 제한자가 다르다.
	- **public** : 모든 외부에서 접근 가능
	- **private** : 같은 클래스에서만 접근 가능
		- **[SerializedField]** : 유니티에서 private로 선언할 시 이 키워드를 사용하면 인스펙터에서는 볼 수 있다.

<br/>

- **Getter / Setter** : private으로 선언된 멤버에 접근하기 위해 만든 메서드
	- 따라서 Getter, Setter 메서드는 public으로 선언
	- **Getter** : private으로 선언된 멤버를 가져와서 사용하기 위한 메서드
	- **Setter** :	private으로 선언된 멤버를 변경 및 수정하기 위한 메서드

#### **예시**
```cpp
class Example
{
private:
	string name;
	int age;
	double height, weight;

public:
	// Getter
	int GetAge()
	{
		return age;
	}
	
	// Setter
	void SetAge(int newAge)
	{
		age = newAge;
	}

	void ShowAge()
	{
		cout << GetAge();
	}
};
```

<br/>

-----------------

<br/>

### **3. 구조체 & 클래스**
- **구조체** : 간단한 데이터의 묶음 (주로 기본 자료형으로 이루어져 있다.)
- **클래스** : 어떤 객체를 다룰 때 사용 (데이터와 기능을 포함)
- `C++`에서는 구조체와 클래스가 큰 차이가 없다.
- 하지만 `C#`에서는 각각 값 형식, 참조 형식이기에 큰 차이가 있다.
	- 구조체는 스택 메모리에, 클래스는 힙 메모리에 저장된다.
	- 또 클래스 안에서 구현된 구조체는 힙 영역에 저장된다. 

<br/>

-----------------

<br/>

### **4. 클래스의 구조**
```cpp
class Example		// 클래스 클래스명 (클래스 선언)
{
public:					// 접근 제한자
	string m_name;	// 멤버 변수
	int m_age;
	float m_height;

	// 생성자
	Example(string name, int age)
	{
		m_name = name;
		m_age = age;
		m_height = 10.0f;
	}
	
	// 소멸자
	~Example(){}

	// 메서드
	void Move(){}
};
```

<br/>

-----------------

<br/>

### **5. 객체 지향 (Object Oriented Programming)**
- 현실의 개체를 객체로 모델링하여 그 객체들의 상호작용으로 프로그래밍하는 방식

#### **객체 지향 4대 특징 (요게 더 중요 : 면접)**
- **상속** : 부모 클래스의 기능을 자식 클래스가 물려받는 것
	- 폴더와 마찬가지이기 때문에 깊이가 너무 깊을 경우 오히려 유지보수가 어려워진다.
- **다형성** : 하나의 이름이 상황에 따라 다르게 동작하는 것 **(오버로딩 & 오버라이딩)**
- **캡슐화** : 데이터와 기능을 하나로 묶고 외부에서 직접 접근하지 못하게 숨기는 것
- **추상화** : 내부 원리를 몰라도 사용할 수 있도록 복잡한 내부 구현을 숨기고 핵심 인터페이스만 보여주는 것

#### **객체지향 5대 원칙 (SOLID)**
- **단일 책임 원칙** : 클래스는 딱 하나의 책임만을 가져야 한다.
- **개방 폐쇄 원칙** : 확장에는 열려있고 수정에는 닫혀있어야 한다.
- **리스코프 치환 원칙** : 자식 클래스는 언제나 부모 클래스를 대체할 수 있어야 한다.
- **인터페이스 분리 원칙** : 자신이 사용하지 않는 기능은 구현하지 않아야 한다.
- **의존 역전 법칙** : 구체적인 것보다 추상적인 것에 의존해야 한다.

<br/>

-----------------

<br/>

### **6. 오버라이딩 & 오버로딩**
- **오버라이드 (Override)** : 자식 클래스에서 부모 클래스의 기능을 덮어쓸 수 있는 기능(메서드 재정의)
	- `C#`에서는 `base` 키워드
	- `virtual` 키워드는 오버라이드를 할 수 있는 함수라고 명시

<br/>

- **오버로드 (Overload)** : 이름과 반환 자료형은 같은 함수이지만 파라미터의 자료형, 개수가 다른 메서드를 만드는 기능 (암기)
	- ex. `Debug.Log("Hello, World!");`, `Debug.Log("Hello, World!", this);`

<br/>

-----------------

<br/>

### **7. 컴포넌트 프로그래밍**
- **컴포넌트 프로그래밍** : 작은 기능을 구현한 각 컴포넌트를 조립해 객체를 만들어내는 프로그래밍 방법
	- 복잡한 상속 관계를 피하기 위해 사용한다.
	- C#, 유니티에서 사용하는 방식이다.

<br/>

-----------------

<br/>

오늘 쫌 어려웠다.... 전공 수업 들을 때도 아마 클래스에서 포기했던 것 같은데ㅋㅋㅋㅋ....

<br/>

-----------------

<br/>
