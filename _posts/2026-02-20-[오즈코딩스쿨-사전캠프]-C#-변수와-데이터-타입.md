---
title : "[오즈코딩스쿨 - 사전캠프] C# 변수와 데이터 타입"
date : 2026-02-20 15:20:00 +0900
categories : [OZ]
tags : [ozcodingschool]
comments : true
pin : true
---

## 사전캠프 - C# 변수와 데이터 타입

### **1. 변수**
**변수**란 데이터를 담을 수 있는 상자와 같다.
원하는 데이터를 변수 안에 담아놓고 사용할 수 있다.

#### **변수 선언 규칙**
변수를 선언하기 위해서는 규칙을 지켜야 한다.

```csharp
// 규칙
데이터타입 변수이름 = 값;

// 예시
string name = "aaa";
int num = 3;
bool isStudent = true;
```

<br/>

-----------------

<br/>

### **2. 데이터 타입**
**데이터 타입**은 그 변수에 어떤 타입의 데이터가 들어가야 하는지 컴퓨터가 알 수 있도록 명시해준다.
데이터 타입과 다른 값이 변수 안에 들어가면 컴파일 단계에서 오류가 발생할 수 있다.

#### **데이타 타입 예시**
| 분류 | 데이터 타입 | 설명 | 
|------|-------------|------|
| 숫자 | `int` | 정수 (소수점 없음) | 10, -5, 0 | 
| 숫자 | `long` | 아주 큰 정수 | 922337203685 | 
| 숫자 | `float` | 소수점 있는 숫자 (4바이트) |
| 숫자 | `double` | 더 정밀한 소수 (8바이트) |
| 숫자 | `decimal` | 초정밀 소수 (돈 계산용) |
| 논리 | `bool` | 참/거짓 |
| 문자 | `char` | 문자 1개 | 
| 문자열 | `string` | 글자들의 모음 |
| 날짜/시간 | `DateTime` | 날짜 + 시간 |
| 없음 | `null` | 값이 없음 (비어 있음) | 
| 기타 | `object` | 모든 타입의 부모 (다 담을 수 있음) |

<br/>

-----------------

<br/>

### **3. 과제**
```
“포도”라는 데이터를 “과일”이라는 변수에 담아 보세요!(데이터 타입 작성 필요)
```

#### **결과**
```csharp
namespace OZcoding
{
    internal class Program
    {
        static void Main(string[] args)
        {
            string 과일 = "포도";

            Console.Write(과일);
        }
    }
}
```

![Desktop View](/assets/img/grape.jpg){: w="500" h="200" }
